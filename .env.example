PORT=8787
DATA_DIR=./data

# Required before saving QQ Bot credentials. Use a long random value and keep it
# unchanged across restarts, otherwise the encrypted AppSecret cannot be read.
QQ_CREDENTIAL_ENCRYPTION_KEY=replace-with-a-long-random-secret
